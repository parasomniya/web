Модели доменной области и БД:

- пользователи и роли;
- рационы (`RationTemplate`);
- группы животных (`AnimalGroup`);
- ингредиенты (`Ingredient`);
- зоны компонентов (`StorageZone`);
- замесы (`Batch`);
- телеметрия (`TelemetryChunk`);
- нарушения и настройки.

Типы данных 
	
telemetry.model.ts — интерфейс {weight, lat, lon}
